# Desafio 04: NAT Gateway + ECS Privado
# Stub — preencher se o desafio rodar BIA localmente
services:
  # bia:
  #   image: henrylle/bia:latest
  #   ports: ["3001:3001"]
  #   environment:
  #     - VITE_API_URL=http://localhost:3001
